#!/bin/sh -exu

./bin/ci.branches

VERSION=$(date '+%Y%m%d%H%M%S')-$(git log --pretty=format:%h -n 1)

cabal haddock --hoogle
aws s3 --region=ap-southeast-2 cp --recursive dist/doc/html/ambiata-tine/ ${AMBIATA_HADDOCK:-s3://ambiata-test-doc}/haddock/ambiata-tine/$VERSION
